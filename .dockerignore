# .git is deliberately not ignored: the build reads the tag out of it to stamp
# the version. It is 8M and never reaches the final image, only the builder.
.github
.gstack
dist
mycelium
apps/client/node_modules
apps/client/build
apps/client/.svelte-kit
